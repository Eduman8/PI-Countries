# API
PORT=3001
NODE_ENV=development
CLIENT_URL=http://localhost:3000

# PostgreSQL - local development
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=countries

# PostgreSQL - deployment/cloud alternative. If set, it takes precedence over DB_* values.
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/countries

# Initial country seed
COUNTRIES_API_URL=https://restcountries.com/v3.1/all
